<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Safety Stats - Women Safety App</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        .card {
            background: #1f2937;
            border-radius: 8px;
            padding: 20px;
            margin-top: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body class="bg-gray-900 text-white h-screen flex flex-col items-center">

    <!-- Page Header -->
    <header class="w-full bg-blue-500 p-4 text-center">
        <h1 class="text-3xl font-bold text-white">Safety Statistics</h1>
        <p class="mt-2 text-lg">View crime trends and safety ratings in your area.</p>
    </header>

    <!-- Main Content -->
    <main class="flex flex-col items-center w-full p-6 space-y-8">

        <!-- Overview Section -->
        <div class="card w-full max-w-3xl mx-auto text-center">
            <h2 class="text-xl font-semibold">Crime Rate Summary</h2>
            <p class="mt-4">Here's an overview of recent safety reports and crime trends.</p>
        </div>

        <!-- Safety Statistics Chart -->
        <div class="card w-full max-w-3xl mx-auto">
            <h2 class="text-xl font-semibold text-center">Crime Trends (Last 6 Months)</h2>
            <canvas id="crimeChart" class="mt-6"></canvas>
        </div>

        <!-- Back to Dashboard -->
        <div class="mt-6">
            <a href="dashboard" class="bg-blue-500 hover:bg-blue-600 text-white px-6 py-3 rounded-lg text-lg font-semibold text-center">Back to Dashboard</a>
        </div>

    </main>

    <!-- Chart.js Script -->
    <script>
        const ctx = document.getElementById('crimeChart').getContext('2d');
        const crimeChart = new Chart(ctx, {
            type: 'line',
            data: {
                labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'],
                datasets: [{
                    label: 'Crime Reports',
                    data: [20, 25, 18, 30, 22, 27],
                    borderColor: '#ff4d4d',
                    backgroundColor: 'rgba(255, 77, 77, 0.2)',
                    borderWidth: 2,
                    fill: true
                }]
            },
            options: {
                responsive: true,
                scales: {
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });
    </script>

</body>
</html>
